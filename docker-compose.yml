services:
  fhir:
    container_name: fhir
    image: hapiproject/hapi:latest
    ports:
      - "8080:8080"
    environment:
      JAVA_TOOL_OPTIONS: "-Xmx8g"
      # Treats US Core URLs as local logical references
      HAPI_FHIR_STORAGE_TREAT_REFERENCES_AS_LOGICAL: "http://hl7.org*"
      # Ensures the IG installer/validator is fully active
      HAPI_FHIR_IG_RUNTIME_UPLOAD_ENABLED: "true"
    volumes:
      - ./hapi.application.yml:/app/config/application.yaml
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped

  db:
    image: postgres:15
    container_name: fhir-db
    environment:
      POSTGRES_DB: hapi
      POSTGRES_USER: hapi
      POSTGRES_PASSWORD: hapi_password
    volumes:
      - pg-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U hapi"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

volumes:
  hapi-data:
  pg-data:
